<!DOCTYPE html>
<html>
<head>
  <title>Track Your Day</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
  <style>
    .card {
      background-color: #ffffff;
      border-radius: 8px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
      max-width: 600px;
      margin: 50px auto;
      padding: 20px 40px;
    }

    .card-2 {
      background-color: black;
      color: white;
      border-radius: 8px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
      max-width: 600px;
      margin: 50px auto;
      padding: 20px 40px;
    }

    .card-title {
      text-align: center;
    }

    .form-group {
      margin-bottom: 20px;
    }

    .form-control {
      width: 100%;
      border: 1px solid #ddd;
      border-radius: 4px;
      padding: 8px;
      font-size: 14px;
    }

    .btn-primary {
      background-color: #000000;
      border: none;
      border-radius: 4px;
      color: white;
      cursor: pointer;
      font-size: 16px;
      padding: 10px 20px;
      text-transform: uppercase;
      width: 100%;
    }

    .btn-primary:hover {
      background-color: #0069d9;
    }
  </style>
</head>
<div class="container">
    <div class="row justify-content-center align-items-center min-vh-100">
      <div class="col-sm-6">
        <div class="card p-4">
          <h1 class="card-title text-center mb-4">Your Journey Starts Here!</h1>
          <%= form_with url: users_path, method: :post, local: true, class: "row g-3" do |f| %>
            <%= f.label :name, "Name:" %>
            <%= f.text_field :name %>

            <%= f.label :email, "Email:" %>
            <%= f.text_field :email %>

            <%= f.label :password, "Set a Password:" %>
            <%= f.text_field :password %>

            <%= f.hidden_field :ip_address, value: @ip_address%>
    
            <div id="protocols">
            <br>
            <br>
            <h4>Select Your Protocol:</h4>
              <%@facade.default_protocols.each do |protocol|%>
                <div class="card-2 p-4">
                  <h5><%=protocol.name%></h5>
                  <br>
                  <p><%=protocol.description%></p>
                  <br>
                  <%= f.label :protocol_id, "Choose #{protocol.name}" %>
                  <%= f.radio_button :protocol_id, protocol.id %>
                  <br>
                </div>
              <%end%>
            </div>

            <%= f.label :data_sharing, "I Elect to Share My Health Trend Data:" %>
            <%= f.select :data_sharing, ['True', 'False'] %>
          <br>
            <%= f.submit " ✨Begin My Journey ✨", class: "btn btn-dark btn-center btn-lg" %>
          <% end %>
          <br>
          <h1 class="text-center">OR...</h1>
          <br>
          <a class="btn btn-dark btn-center btn-sm text-nowrap" href='/auth/google_oauth2'>Log In with Google</a>
        </div>
      </div>
    </div>
  </div>