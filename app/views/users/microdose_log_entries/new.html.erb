<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Track Your Day</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        background-color: #F0F0F0;
      }
      .container {
        margin-top: 50px;
      }
      .card {
        background-color: #FFFFFF;
        border-radius: 8px;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        padding: 20px 40px;
      }
      .card-title {
        margin-top: 0;
        margin-bottom: 20px;
      }
      .form-label {
        font-weight: bold;
        margin-bottom: 5px;
      }
      .form-control {
        width: 100%;
        border: 1px solid #ddd;
        border-radius: 4px;
        padding: 8px;
        font-size: 14px;
        margin-bottom: 20px;
      }
      .btn {
        border: none;
        border-radius: 4px;
        cursor: pointer;
        font-size: 16px;
        text-transform: uppercase;
      }
      .btn-dark {
        background-color: #000000;
        color: white;
      }
      .btn-dark:hover {
        background-color: #45A049;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="row justify-content-center align-items-center min-vh-100">
        <div class="col-sm-6">
          <div class="card p-4">
            <h1 class="card-title text-center mb-4">Track Your Day</h1>
            <%= form_with url: user_microdose_log_entries_path(@facade.user_id), method: :post, class: "row g-3" do |f| %>
              <div class="col-md-12">
                <%= f.label :mood_before, "Mood Before:", class: "form-label" %>
                <%= f.text_field :mood_before, class: "form-control" %>
              </div>
              <div class="col-md-12">
                <%= f.label :mood_after, "Mood After:", class: "form-label" %>
                <%= f.text_field :mood_after, class: "form-control" %>
              </div>
              <div class="col-md-12">
                <%= f.label :environment, "Environment:", class: "form-label" %>
                <%= f.text_field :environment, class: "form-control" %>
              </div>
              <div class="col-md-12">
                <%= f.label :dosage, "Dosage (g):", class: "form-label" %>
                <%= f.text_field :dosage, value: '0.00', class: "form-control" %>
              </div>
              <div class="col-md-12">
                <%= f.label :intensity, "Intensity:", class: "form-label" %>
                <%= f.select :intensity, ['low', 'medium', 'high'], class: "form-control" %>
              </div>
              <div class="col-md-12">
                <%= f.label :sociability, "Sociability:", class: "form-label" %>
              <%= f.select :sociability, ['social', 'anxious', 'withdrawn'], class: "form-control" %>
            </div>
            <br>
            <br>
            <div id="openai">
              <p><%= @openai %></p>
            </div>
            <div class="col-md-12">
              <%= f.label :journal_entry, "Write your response here:", class: "form-label" %>
              <div class="form-control">
                <%= f.text_area :journal_entry, :cols => "65", :rows => "10" %>
              </div>
            </div>
            <div class="col-md-12 mt-4">
              <%= f.label :other_notes, "Other Notes:", class: "form-label" %>
              <div class="form-control">
                <%= f.text_area :other_notes, :cols => "65", :rows => "10" %>
              </div>
            </div>
            <div class="col-md-12">
              <%= f.submit "Upload to the Universe", class: "btn btn-dark w-100 mb-3 text-white" %>
            </div>
          <% end %>
        </div>
      </div>
    </div>
  </div>